import { VocabularyScreen } from '@/features/vocabulary/VocabularyScreen';

/** I tag del gruppo. La schermata è una sola: vedi `VocabularyScreen`. */
export default function TagsScreen() {
  return <VocabularyScreen kind="tag" />;
}
